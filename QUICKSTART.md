# دليل البدء السريع

## خطوات سريعة للتشغيل

### 1. تثبيت Node.js
تأكد من تثبيت Node.js (الإصدار 16 أو أحدث) على جهازك.

### 2. تثبيت الحزم

```bash
npm install
```

### 3. تشغيل المشروع

```bash
npm run dev
```

سيتم تشغيل المشروع على `http://localhost:5173`

---

## إذا واجهت مشاكل

### مشكلة في npm install
جرب:
```bash
npm cache clean --force
npm install
```

### مشكلة في البورت
إذا كان البورت 5173 مشغول، سيقوم Vite تلقائياً باستخدام بورت آخر.

---

## ملاحظات مهمة

- **الوضع التطويري**: التطبيق حالياً يعمل في وضع التطوير مع بيانات وهمية
- **البيانات**: جميع البيانات المعروضة هي أمثلة ولا تعكس بيانات حقيقية من البلوكشين
- **التصميم**: التصميم مستوحى من Etherscan ولكن بمكونات مخصصة

---

## الخطوات التالية

بعد تشغيل المشروع، يمكنك:

1. **إضافة API حقيقي**: ربط المشروع بـ API لجلب بيانات حقيقية
2. **إضافة صفحات جديدة**: مثل صفحة البلوك، صفحة المعاملة، إلخ.
3. **تحسين التصميم**: تخصيص الألوان والأنماط حسب رغبتك
4. **إضافة المزيد من المميزات**: مثل الرسوم البيانية، البحث المتقدم، إلخ.

---

## الأوامر المفيدة

```bash
# تشغيل وضع التطوير
npm run dev

# بناء للإنتاج
npm run build

# معاينة البناء
npm run preview
```

---

## هيكل المشروع

```
📁 blockchain-explorer/
├── 📁 src/
│   ├── 📁 components/      # المكونات القابلة لإعادة الاستخدام
│   │   ├── TopBar.jsx
│   │   ├── Header.jsx
│   │   ├── Hero.jsx
│   │   ├── StatsSection.jsx
│   │   ├── LatestBlocks.jsx
│   │   ├── LatestTransactions.jsx
│   │   └── Footer.jsx
│   ├── App.jsx            # المكون الرئيسي
│   ├── main.jsx          # نقطة الدخول
│   └── index.css         # الأنماط الرئيسية
├── index.html
├── package.json
├── vite.config.js
└── tailwind.config.js
```

---

## تخصيص الألوان

لتغيير الألوان الرئيسية، قم بتعديل ملف `tailwind.config.js`:

```javascript
theme: {
  extend: {
    colors: {
      primary: {
        500: '#0ea5e9',  // لونك المفضل هنا
        600: '#0284c7',
        700: '#0369a1',
      },
    },
  },
}
```

---

## الدعم

إذا كنت بحاجة إلى مساعدة:
- راجع الملف README.md
- افتح issue على GitHub
- راسل المطور

---

🎉 **مبروك! مشروعك جاهز الآن!** 🎉

